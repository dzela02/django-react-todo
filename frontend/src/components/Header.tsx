import { Typography } from "@material-ui/core";

const Header = () => {
  return (
    <div>
      <Typography variant="h3" align="center">
        TODO App
      </Typography>
    </div>
  );
};

export default Header;
